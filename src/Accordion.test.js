// import React from 'react';
// import { shallow } from 'enzyme';

// import Accordion from './Accordion';

describe('Accordion', () => {
  it('shallow render', () => {
    expect(1).toBe(1);
  });
});
